#!/usr/bin/env python3
"""Try finalization WITHOUT bit length in padding."""

SB = []

L_VEC = [
    0x94141c3c08421001, 0x8020080020501080,
    0x4010104000020488, 0x0800202000808802,
    0x8020880140000000, 0x0100008004000840,
    0x8080010000802010, 0x8000010002010401
]
POLY = 0x1C3

def gf_mul64(a, b):
    r = 0
    for _ in range(64):
        if b & 1: r ^= a
        hi = a >> 63
        a = (a << 1) & 0xFFFFFFFFFFFFFFFF
        if hi: a ^= POLY
        b >>= 1
    return r

def bytes_to_int_be(b):
    r = 0
    for byte in b: r = (r << 8) | byte
    return r

def to_bytes_be(n, length):
    return bytes([(n >> (8*(length-1-i))) & 0xFF for i in range(length)])

def to_bytes_le(n, length):
    return bytes([(n >> (8*i)) & 0xFF for i in range(length)])

def l_transform(block):
    qws = [bytes_to_int_be(block[i*8:(i+1)*8]) for i in range(8)]
    result = 0
    for i in range(8):
        result ^= gf_mul64(qws[i], L_VEC[i])
    return result

def R_right(block):
    l_val = l_transform(block)
    new_block = bytearray(64)
    for j in range(8): new_block[j] = (l_val >> (8*(7-j))) & 0xFF
    for i in range(56): new_block[8+i] = block[i]
    return bytes(new_block)

def L_func(block):
    for _ in range(8): block = R_right(block)
    return block

def Vec_512(j):
    result = bytearray(64)
    j_bytes = to_bytes_be(j, 8)
    for i in range(8): result[56+i] = j_bytes[i]
    return bytes(result)

C = [L_func(Vec_512(i+1)) for i in range(12)]

def apply_S(block):
    return bytes(SB[b] for b in block)

def rotate_left(block, bits):
    rot = bits // 8
    return bytes(block[(i + rot) % 64] for i in range(64))

def streebog_g(h, m, n):
    k = bytes(a ^ b for a, b in zip(h, n))
    u = bytes(a ^ b for a, b in zip(h, m))
    for r in range(12):
        w = bytes(a ^ b for a, b in zip(k, C[r]))
        w = apply_S(w)
        w = L_func(w)
        k = w
        v = bytes(a ^ b for a, b in zip(u, C[r]))
        v = apply_S(v)
        v = L_func(v)
        u = v
        k = rotate_left(k, (11 - r) * 8)
    return bytes(a ^ b ^ c for a, b, c in zip(h, u, k))

def add512(a, b):
    result = bytearray(64)
    carry = 0
    for i in range(63, -1, -1):
        s = a[i] + b[i] + carry
        result[i] = s & 0xFF
        carry = s >> 8
    return bytes(result)

def streebog256(data):
    h = bytes([0x01] * 64)
    sigma = bytes(64)
    byte_count = 0
    offset = 0
    while offset + 64 <= len(data):
        m = data[offset:offset+64]
        byte_count += 64
        n = bytearray(64)
        n[:16] = to_bytes_le(byte_count * 8, 16)
        sigma = add512(sigma, m)
        h = streebog_g(h, m, bytes(n))
        offset += 64
    remaining = data[offset:]
    buf_len = len(remaining)
    byte_count += buf_len

    # Pad: just 0x01 + zeros, NO bit length in padding
    padded = bytearray(64)
    padded[:buf_len] = remaining
    padded[buf_len] = 0x01

    n_bytes = to_bytes_le(byte_count * 8, 16)
    n = bytearray(64)
    n[:16] = n_bytes

    sigma = add512(sigma, bytes(padded))
    h = streebog_g(h, bytes(padded), bytes(n))

    h = streebog_g(h, bytes(64), bytes(n))
    h = streebog_g(h, sigma, bytes(64))
    return h[:32]

data = bytes.fromhex(
    "00112233445566778899aabbccddeeff00112233445566778899aabbccddeeff"
    "ff00ee11dd22cc33bb44aa5599668877"
    "8899aabbccddeeff88889999aaaabbbbccccddddeeeeffff89abcdeffedcba98"
    "0011223344556677000011112222333344445555666677770123456776543210"
)
h = streebog256(b'')
print(f"Empty:  {''.join(f'{b:02x}' for b in h)}")
h = streebog256(data)
print(f"HLS9_C: {''.join(f'{b:02x}' for b in h)}")
print()
print("Expected Empty:  3f5b11e2a8c30975dc351857a5f5593271c4d34499eaff0e8459894c5a896e47")
print("Expected HLS9_C: 4c375b843898b6f0a0744051f74e42f2a944581d46c495e743e97abdcd9d7c58")
